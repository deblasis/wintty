<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Ghostty.Controls.Settings.SettingsCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
      Atomic row of a settings page. Label + description on the left,
      control slot on the right. Height and spacing are hard-coded here
      so every row on every page aligns the same way, eliminating the
      drift we had before where each page used ad-hoc StackPanel Spacing
      values. The ConfigKey attached property lets settings search scroll
      to + pulse a specific card.
    -->
    <Border
        Padding="16,10"
        MinHeight="68"
        CornerRadius="{ThemeResource ControlCornerRadius}"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        Margin="0,0,0,6">
        <Grid ColumnSpacing="16" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <!--
                  MinWidth keeps the label readable when Control is a wide
                  Auto stack (profile rows: icon picker + two ToggleSwitches).
                  Without it the star column collapses to ~1ch and wraps
                  the header vertically.
                -->
                <ColumnDefinition Width="*" MinWidth="200" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="2">
                <TextBlock
                    x:Name="HeaderText"
                    Style="{StaticResource BodyStrongTextBlockStyle}"
                    TextWrapping="Wrap" />
                <TextBlock
                    x:Name="DescriptionText"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap"
                    Visibility="Collapsed" />
            </StackPanel>

            <ContentPresenter
                x:Name="ControlPresenter"
                Grid.Column="1"
                VerticalAlignment="Center"
                HorizontalAlignment="Right" />
        </Grid>
    </Border>
</UserControl>
