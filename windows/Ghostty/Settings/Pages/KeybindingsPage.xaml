<Page
    x:Class="Ghostty.Settings.Pages.KeybindingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:settings="using:Ghostty.Settings">

    <Page.Resources>
        <!--
          One template for header + entry. A per-type template selector over
          Ghostty.Core row types InvalidCastExceptions in MeasureOverride.
          ContainerContentChanging toggles which child is visible.
        -->
        <DataTemplate x:Key="KeybindRowTemplate">
            <Grid>
                <TextBlock x:Name="HeaderText"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           Margin="0,12,0,4"
                           Visibility="Collapsed" />
                <Grid x:Name="EntryGrid" ColumnDefinitions="*,Auto,Auto,Auto" Padding="8,4" Visibility="Collapsed">
                    <Grid.ContextFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem x:Name="RebindItem" Text="Rebind..." Click="RebindItem_Click" />
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem x:Name="UnbindItem" Text="Unbind" Click="UnbindItem_Click" />
                            <MenuFlyoutItem x:Name="ResetItem" Text="Reset to default" Click="ResetItem_Click" />
                        </MenuFlyout>
                    </Grid.ContextFlyout>
                    <TextBlock x:Name="FriendlyText" VerticalAlignment="Center" />
                    <FontIcon x:Name="ConflictIcon"
                              Grid.Column="1"
                              Glyph="&#xE7BA;"
                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              Foreground="{ThemeResource SystemFillColorCautionBrush}"
                              FontSize="14"
                              Margin="0,0,8,0"
                              VerticalAlignment="Center"
                              Visibility="Collapsed" />
                    <Border x:Name="UserTag"
                            Grid.Column="2"
                            Background="{ThemeResource SubtleFillColorTertiaryBrush}"
                            CornerRadius="4" Padding="6,1"
                            Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                        <TextBlock Text="User" FontSize="11"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </Border>
                    <Border Grid.Column="3"
                            Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                            CornerRadius="4" Padding="8,2">
                        <TextBlock x:Name="LabelText"
                                   FontFamily="Cascadia Code, Consolas, monospace"
                                   VerticalAlignment="Center" />
                    </Border>
                </Grid>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="24" RowDefinitions="Auto,Auto,*">
        <TextBlock Text="Keybindings" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right" VerticalAlignment="Center">
            <SelectorBar x:Name="ViewBar" SelectionChanged="ViewBar_SelectionChanged">
                <SelectorBarItem x:Name="ListBarItem" Text="List" IsSelected="True" />
                <SelectorBarItem x:Name="KeyboardBarItem" Text="Keyboard" />
            </SelectorBar>
            <Button x:Name="CheatSheetButton" Content="Cheat sheet" Click="CheatSheetButton_Click" />
        </StackPanel>

        <Grid x:Name="ListPanel" Grid.Row="1" Grid.RowSpan="2" RowDefinitions="Auto,*">
            <Grid ColumnDefinitions="*,Auto" Margin="0,0,0,16">
                <AutoSuggestBox x:Name="SearchBox" PlaceholderText="Search by action or key..."
                                QueryIcon="Find" TextChanged="SearchBox_TextChanged" />
                <ToggleButton x:Name="ConflictsToggle" Grid.Column="1" Content="Conflicts"
                              Margin="8,0,0,0" Checked="ConflictsToggle_Toggled"
                              Unchecked="ConflictsToggle_Toggled" />
            </Grid>
            <ListView x:Name="BindingsList" Grid.Row="1" SelectionMode="None"
                      ItemTemplate="{StaticResource KeybindRowTemplate}" />
        </Grid>

        <ScrollViewer x:Name="KeyboardPanel" Grid.Row="1" Grid.RowSpan="2" Visibility="Collapsed">
            <settings:KeyboardMapView x:Name="Map" Margin="0,8,0,0" />
        </ScrollViewer>
    </Grid>
</Page>
